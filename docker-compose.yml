version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - USER_TOKEN=your_cloudru_token
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"